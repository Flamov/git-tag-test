name: test2

on:
  workflow_dispatch:
    inputs:
      environment:
        description: Environment Name
        required: true
        default: dev7
  push:
    branches:
      - feature/test2

jobs:
  test2:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Get git hash
        id: hash
        run: echo "::set-output name=hash::$(git rev-parse --short=7 ${{ github.sha }})"

      - name: Extract branch name
        id: branch
        run: echo "::set-output name=branch::$(BRANCH=${${{ github.ref }}#refs/heads/}; echo ${BRANCH////-})"

      - name: Log
        run: echo "${{ github.event.inputs.environment }}-${{ steps.hash.outputs.hash }}-${{ steps.branch.outputs.branch }}"

